export const DB_NAME = "HireSenseAI";

// The live frontend URL for CORS, redirects, etc.
// In production, this MUST be set as an environment variable.
export const PRODUCTION_URL = process.env.FRONTEND_URL || "http://localhost:5173";